{
  "name": "claude_code",
  "label": "Claude Code",
  "description": "Claude AI — powers the agent",
  "auth": {
    "type": "setup_token",
    "env_key": "ANTHROPIC_AUTH_TOKEN",
    "setup_note": "Run `claude setup-token` on your local machine (where you're logged in to Claude). Paste the sk-ant-oat01-... token here. Uses your Claude Pro/Max subscription instead of API credits.",
    "fallback_note": "No token set — currently using developer account."
  },
  "restart_service": "second-brain-bot",
  "pipelines": [],
  "health": {
    "type": "command",
    "command": ["claude", "auth", "status"],
    "expect_exit_code": 0
  }
}
